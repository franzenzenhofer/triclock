export interface TimeValues {
  readonly hours: number;
  readonly minutes: number;
  readonly seconds: number;
  readonly ms: number;
}

export interface FractionalTime {
  readonly h: number;
  readonly m: number;
  readonly s: number;
}
